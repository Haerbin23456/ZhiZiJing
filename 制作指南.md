# 智姿镜项目制作指南

本文档用于指导 AI 或开发者从零完成《智姿镜：多手机协同运动姿态评估系统》。阅读本文档后，应当能够按照阶段逐步完成安卓原生 APP、姿态识别与规则评估、独立动作分类器训练、模型接入、测试验收和课程展示。

项目默认技术路线为 **Kotlin + XML + ViewBinding + Activity/MVVM**。不要把大模型 API 当作核心功能，不要训练人体关键点检测模型，人体关键点检测直接使用 **ML Kit Pose Detection**。如果后期要做模型训练，只训练“基于人体关键点序列的动作分类器”，并且把它设计成可插拔加分模块，不能阻塞主 APP 开发。

## 1. 项目目标与验收优先级

### 1.1 项目一句话目标

开发一款安卓原生 APP，使用多台手机组成正面和侧面机位，通过 CameraX 采集画面，使用 ML Kit Pose Detection 识别人体关键点，完成深蹲计数、深蹲标准度评估、训练结果保存和报告展示；在此基础上扩展开合跳计数、多机位融合、PDF/JSON 导出和动作分类器接入。

### 1.2 必须完成的 MVP

MVP 的目标是保证课程验收时可以稳定演示，优先级高于模型训练和复杂 UI。

必须完成以下功能：

1. 登录注册和自动登录。
2. 训练首页，展示最近训练、深蹲入口、开合跳入口、创建房间、加入房间、历史记录入口。
3. 创建训练房间和加入训练房间。
4. 多设备组队，主控端能看到节点设备。
5. 主控端能分配正面机位、侧面机位。
6. 摄像头节点能使用 CameraX 预览画面。
7. 节点端能使用 ML Kit Pose Detection 识别人体关键点。
8. 节点端能绘制人体骨架线。
9. 深蹲能实时计数。
10. 深蹲能判断下蹲深度、膝盖内扣、背部前倾、左右对称等问题。
11. 主控端能展示深蹲次数、当前评分、问题提示。
12. 训练结束后能进入结果页。
13. 训练记录能保存到 Room，并能在历史记录页面查看。

### 1.3 加分功能

加分功能在 MVP 稳定后再做，不要一开始就把项目复杂化。

推荐加分顺序：

1. 开合跳计数：只做打开/合拢识别和次数统计，不做复杂评分。
2. PDF/JSON 报告导出：方便验收展示。
3. 多机位结果融合：正面机位与侧面机位共同生成最终结果。
4. 异常降级：节点掉线或识别失败时提示用户，并降级为单机位分析。
5. 动作分类器模型接入：自动识别深蹲、开合跳、未知动作。

### 1.4 不要做的事情

1. 不要训练人体姿态关键点检测模型，直接使用 ML Kit Pose Detection。
2. 不要把纯大模型 API 调用当作项目核心。
3. 不要先做复杂模型训练再做 APP。
4. 不要把所有功能堆到一个 Activity。
5. 不要为了 UI 好看牺牲功能闭环。
6. 不要把图片、视频直接存入 Room 数据库。
7. 不要保存每一帧姿态数据，只保存关键帧、问题帧、抽样帧或最终分析结果。

## 2. AI / Vibe Coding 工作原则

### 2.1 每次让 AI 开发前先声明上下文

每次启动新的 AI 编码会话时，先让 AI 阅读本文档，并明确当前阶段。例如：

```text
请先阅读 制作指南.md。当前阶段是完成单机 CameraX + ML Kit 姿态识别，不要开发多设备通信，不要训练模型。请按文档中的包结构和接口设计实现。
```

### 2.2 每次只做一个阶段

不要一次让 AI 完成整个项目。推荐按下面节奏推进：

1. 先搭项目和基础依赖。
2. 再做登录、首页、数据库。
3. 再做单机 CameraX 预览。
4. 再做 ML Kit 关键点识别和骨架绘制。
5. 再做深蹲状态机与评分。
6. 再做 Nearby 多设备连接。
7. 再做主控和节点联动。
8. 再做结果页、历史记录和报告。
9. 最后做分类器训练与接入。

### 2.3 每次开发都要求 AI 给出验收方式

每完成一个模块，必须让 AI 同时给出：

1. 修改了哪些文件。
2. 这个模块如何运行。
3. 需要授予哪些权限。
4. 预期看到什么现象。
5. 如果失败，优先检查什么。

### 2.4 保持模块边界清晰

项目至少分为以下模块：

1. 页面层：Activity、Fragment、Adapter、XML 布局。
2. 数据层：Room Entity、Dao、Database、Repository、DataStore。
3. 姿态层：CameraX、ML Kit、PoseFrame、骨架绘制。
4. 动作分析层：深蹲状态机、开合跳计数、评分规则。
5. 通信层：Nearby Connections、房间、设备、指令、心跳。
6. 报告层：训练结果汇总、JSON 导出、PDF 导出。
7. 模型层：规则分类器、TFLite 分类器、统一分类接口。

AI 写代码时必须先判断当前任务属于哪个模块，不能把无关逻辑混在页面里。

## 3. 总体开发路线

### 3.1 两条主线

本项目分为两条主线。

第一条是 **应用主体开发主线**。这条线负责完成安卓 APP 的全部可演示功能，包括页面、数据库、CameraX、ML Kit、Nearby Connections、深蹲评估、开合跳计数、结果保存和报告导出。

第二条是 **独立分类器训练主线**。这条线只负责训练动作分类器，用关键点序列判断当前动作属于深蹲、开合跳还是未知动作。分类器训练必须独立于 APP 主线，APP 主线不依赖它也能演示。

### 3.2 推荐阶段顺序

阶段 0：创建项目与基础依赖。

阶段 1：登录、首页、Room、DataStore、历史记录基础表。

阶段 2：单机 CameraX 预览。

阶段 3：ML Kit Pose Detection 姿态关键点识别和骨架绘制。

阶段 4：深蹲规则评估，完成计数、评分、问题提示。

阶段 5：开合跳基础计数。

阶段 6：Nearby Connections 多设备组队和机位分配。

阶段 7：主控端和节点端通信，完成同步倒计时和训练指令。

阶段 8：多机位结果融合，训练结果页，历史记录，报告导出。

阶段 9：独立训练动作分类器。

阶段 10：把分类器接入 APP，但保留规则分类器作为 fallback。

### 3.3 每阶段完成标准

每个阶段都必须达到“能运行、能截图、能解释”的标准。

能运行：真机或模拟器能启动，不崩溃。

能截图：关键页面能截图放入验收材料。

能解释：能说清这个功能的技术实现、输入输出和得分点。

## 4. 应用主体开发流程

### 4.1 创建 Android 项目

创建一个 Android 原生项目，默认使用：

1. Kotlin。
2. XML 布局。
3. ViewBinding。
4. Activity + ViewModel。
5. minSdk 根据 CameraX、ML Kit、Nearby Connections 要求选择，建议不低于 23。
6. compileSdk 使用当前 Android Studio 支持的稳定版本。

依赖方向：

1. AndroidX AppCompat 或 Material Components。
2. ConstraintLayout。
3. RecyclerView。
4. Lifecycle ViewModel 和 LiveData 或 StateFlow。
5. Room。
6. DataStore Preferences。
7. CameraX core、camera2、lifecycle、view、video。
8. ML Kit Pose Detection。
9. ML Kit Barcode Scanning 或 ZXing。
10. Google Nearby Connections。
11. Gson 或 Moshi。
12. TensorFlow Lite，后期接入分类器时再加入。

不要在第一天就接入所有依赖。先接入基础 UI、Room、DataStore，再接入 CameraX 和 ML Kit，最后接入 Nearby 和 TFLite。

### 4.2 推荐包结构

包结构建议如下：

```text
com.example.zhizijing
├── ui
│   ├── auth
│   ├── main
│   ├── room
│   ├── device
│   ├── prepare
│   ├── camera
│   ├── analysis
│   ├── result
│   └── history
├── data
│   ├── local
│   ├── datastore
│   ├── entity
│   ├── dao
│   ├── repository
│   └── mapper
├── domain
│   ├── model
│   ├── usecase
│   └── rule
├── pose
│   ├── detector
│   ├── model
│   ├── overlay
│   ├── feature
│   └── classifier
├── nearby
│   ├── connection
│   ├── message
│   ├── room
│   └── heartbeat
├── report
│   ├── json
│   └── pdf
└── utils
```

如果 AI 想简化包结构，可以减少子包，但不能把姿态识别、数据库、通信和页面逻辑全部堆在一个目录。

### 4.3 页面清单和职责

#### LoginActivity / RegisterActivity

职责：完成账号注册、登录和自动登录。

输入：用户名、密码、昵称。

输出：当前登录用户 ID，写入 DataStore。

验收标准：

1. 能注册新用户。
2. 能登录已有用户。
3. 重启 APP 后能保持登录状态。
4. 登录成功后进入 MainActivity。

#### MainActivity

职责：训练首页和功能入口。

显示内容：

1. 最近一次训练结果。
2. 深蹲平均评分。
3. 开合跳累计次数。
4. 创建训练房间按钮。
5. 加入训练房间按钮。
6. 深蹲评估入口。
7. 开合跳计数入口。
8. 历史记录入口。

验收标准：

1. 页面能从数据库读取最近训练。
2. 点击入口能跳转到对应页面。
3. 没有历史记录时显示空状态。

#### RoomActivity / JoinRoomActivity

职责：创建或加入训练房间。

主控端：

1. 生成房间码。
2. 展示二维码。
3. 开启 Nearby advertising。
4. 等待节点加入。

节点端：

1. 输入房间码或扫码。
2. 开启 Nearby discovery。
3. 连接到主控设备。

验收标准：

1. 两台手机能发现并连接。
2. 主控端能显示节点名称。
3. 节点端能显示加入成功。

#### DeviceGroupActivity

职责：显示设备列表并分配机位。

显示内容：

1. 房间码。
2. 已连接设备数量。
3. 主控手机。
4. 正面机位手机。
5. 侧面机位手机。
6. 在线状态。
7. 电量。
8. 网络延迟。
9. 机位角色。

操作：

1. 分配为正面机位。
2. 分配为侧面机位。
3. 重新分配机位。
4. 开始同步倒计时。

验收标准：

1. 节点角色能同步到节点端。
2. 节点端能显示自己的角色。
3. 主控端能看到在线、电量、延迟。

#### PrepareActivity

职责：训练前准备和同步倒计时。

显示内容：

1. 当前训练动作。
2. 正面机位摆放提示。
3. 侧面机位摆放提示。
4. 人体入镜提示。
5. 3 秒倒计时。

验收标准：

1. 主控点击开始后，所有节点收到倒计时指令。
2. 倒计时结束后进入采集状态。
3. 用户能明确知道手机应该放在哪里。

#### CameraNodeActivity

职责：节点端摄像头预览、姿态检测、骨架绘制和数据上传。

核心功能：

1. 申请相机权限。
2. CameraX Preview。
3. CameraX ImageAnalysis。
4. ML Kit Pose Detection。
5. 骨架 Overlay。
6. 将 PoseFrame 或分析摘要发送给主控端。

验收标准：

1. 真机能打开摄像头。
2. 人体入镜时能显示骨架线。
3. 关键点置信度不足时能提示调整站位。
4. 训练结束后能释放摄像头资源。

#### SquatAnalysisActivity

职责：主控端实时深蹲评估。

显示内容：

1. 当前次数。
2. 当前阶段。
3. 膝关节角度。
4. 下蹲深度。
5. 背部前倾状态。
6. 膝盖内扣状态。
7. 当前评分。
8. 问题提示。
9. 结束训练按钮。

验收标准：

1. 完成一次深蹲后次数加一。
2. 下蹲太浅能提示“下蹲深度不足”。
3. 膝盖明显向内时能提示“膝盖内扣”。
4. 背部前倾明显时能提示“背部前倾”。
5. 结束训练后进入结果页。

#### JumpingJackActivity

职责：开合跳计数。

显示内容：

1. 当前次数。
2. 训练时长。
3. 平均节奏。
4. 人体识别状态。

验收标准：

1. 完成一次开合跳后次数加一。
2. 人体丢失时暂停计数并提示。
3. 训练结束后保存结果。

#### ResultActivity

职责：展示训练结果和改进建议。

深蹲结果：

1. 总次数。
2. 平均评分。
3. 合格次数。
4. 主要问题。
5. 改进建议。
6. 正面关键帧。
7. 侧面关键帧。

开合跳结果：

1. 总次数。
2. 训练时长。
3. 平均节奏。
4. 中断次数。

验收标准：

1. 能展示刚刚完成的训练结果。
2. 能保存到历史记录。
3. 能导出 JSON 或 PDF。

#### HistoryActivity

职责：训练历史记录。

功能：

1. 按日期查看。
2. 按动作类型筛选。
3. 点击进入详情。
4. 删除训练记录。

验收标准：

1. 训练结束后历史记录出现新数据。
2. 点击记录能查看详情。
3. 删除后数据库同步更新。

## 5. 数据库、配置和文件存储

### 5.1 Room 数据表

#### User

用途：保存本地用户账号。

字段建议：

```text
userId: Long, 主键, 自动生成
username: String, 非空, 唯一
passwordHash: String, 非空
nickname: String?
createdAt: Long
lastLoginAt: Long?
```

#### TrainingSession

用途：保存一次训练任务总览。

字段建议：

```text
sessionId: Long, 主键, 自动生成
userId: Long
actionType: String
startTime: Long
endTime: Long?
deviceCount: Int
totalCount: Int
averageScore: Float?
mainProblems: String?
reportPath: String?
status: String
```

#### DeviceNode

用途：保存训练中参与协作的设备。

字段建议：

```text
nodeId: Long, 主键, 自动生成
sessionId: Long
deviceName: String
endpointId: String?
role: String
batteryLevel: Int?
networkDelayMs: Int?
isOnline: Boolean
lastHeartbeatAt: Long?
```

#### PoseFrame

用途：保存关键姿态帧。不要保存每一帧，只保存关键帧、问题帧或抽样帧。

字段建议：

```text
frameId: Long, 主键, 自动生成
sessionId: Long
nodeId: Long
timestampMs: Long
cameraRole: String
landmarksJson: String
confidence: Float?
frameImagePath: String?
frameType: String
```

#### ActionResult

用途：保存每一次动作分析结果。

字段建议：

```text
resultId: Long, 主键, 自动生成
sessionId: Long
actionIndex: Int
actionType: String
startTimeMs: Long
endTimeMs: Long
score: Float?
kneeAngle: Float?
trunkAngle: Float?
depthLevel: String?
problemType: String?
suggestion: String?
keyFramePath: String?
```

#### ExportRecord

用途：保存导出记录。

字段建议：

```text
exportId: Long, 主键, 自动生成
sessionId: Long
exportType: String
filePath: String
createdAt: Long
fileSize: Long?
```

### 5.2 DataStore 配置

DataStore 保存轻量级配置，不保存大量训练数据。

推荐键：

```text
isLoggedIn: Boolean
lastUserId: Long
defaultActionType: String
lastRoomCode: String
minPoseConfidence: Float
squatDepthThreshold: Float
backLeanThreshold: Float
actionRecognitionThreshold: Float
showSkeletonOverlay: Boolean
saveVideoEnabled: Boolean
defaultExportDir: String
```

### 5.3 本地文件目录

训练文件不要直接存入数据库，推荐目录：

```text
files/training/{sessionId}/frames/
files/training/{sessionId}/videos/
files/training/{sessionId}/reports/
files/training/{sessionId}/raw_pose/
```

关键帧命名示例：

```text
problem_squat_005_knee_valgus_front.jpg
problem_squat_005_shallow_depth_side.jpg
best_squat_003_side.jpg
```

JSON 报告命名示例：

```text
training_1001_summary.json
training_1001_pose_samples.json
```

PDF 报告命名示例：

```text
training_1001_report.pdf
```

## 6. 姿态识别与规则评估流程

### 6.1 基本原则

ML Kit Pose Detection 负责识别人体关键点，不负责直接告诉 APP 用户正在做什么动作，也不负责评分。APP 需要基于关键点结果完成动作识别、状态机、计数和评分。

第一版不要依赖训练分类器。先用规则跑通深蹲和开合跳。

### 6.2 PoseFrame 统一数据结构

所有姿态识别结果都统一封装成 PoseFrame。不要在页面里直接传 ML Kit 原始对象。

推荐字段：

```text
sessionId: Long
nodeId: Long
timestampMs: Long
cameraRole: FRONT_CAMERA / SIDE_CAMERA / SINGLE_CAMERA
landmarks: Map<String, LandmarkPoint>
overallConfidence: Float
imageWidth: Int
imageHeight: Int
```

LandmarkPoint 推荐字段：

```text
name: String
x: Float
y: Float
z: Float?
confidence: Float
```

常用关键点：

```text
LEFT_SHOULDER
RIGHT_SHOULDER
LEFT_ELBOW
RIGHT_ELBOW
LEFT_WRIST
RIGHT_WRIST
LEFT_HIP
RIGHT_HIP
LEFT_KNEE
RIGHT_KNEE
LEFT_ANKLE
RIGHT_ANKLE
LEFT_FOOT_INDEX
RIGHT_FOOT_INDEX
```

### 6.3 关键点过滤

每帧进入分析前先检查置信度。

建议规则：

1. 关键点置信度低于 `minPoseConfidence` 时，不参与评分。
2. 深蹲侧面分析至少需要肩、髋、膝、踝。
3. 深蹲正面分析至少需要左右髋、左右膝、左右踝。
4. 开合跳分析至少需要左右手腕、左右肩、左右脚踝。
5. 连续丢失人体超过 1 秒，页面显示“请保持全身入镜”。

### 6.4 角度和距离工具

在 `pose.feature` 中提供通用计算工具。

必须实现：

1. 三点夹角计算：用于膝关节角度、髋关节角度。
2. 两点距离计算：用于脚踝距离、手腕距离。
3. 点到垂直线偏移：用于膝盖内扣。
4. 身体中心线：用于左右对称性。
5. 坐标归一化：用肩宽、髋宽或身高估算值归一化距离，减少不同手机距离和画面大小影响。

### 6.5 深蹲状态机

深蹲用状态机识别，不要只根据单帧判断。

状态：

```text
STANDING
DESCENDING
SQUATTING
RISING
```

状态变化：

```text
STANDING -> DESCENDING
DESCENDING -> SQUATTING
SQUATTING -> RISING
RISING -> STANDING
```

计数规则：

1. 初始为 STANDING。
2. 膝角变小、髋部下降，进入 DESCENDING。
3. 到达最低点或膝角低于阈值，进入 SQUATTING。
4. 膝角变大、髋部上升，进入 RISING。
5. 回到接近站立姿态，计数加一并回到 STANDING。

防抖规则：

1. 每个状态至少持续若干帧后才允许切换。
2. 两次计数之间设置最小时间间隔。
3. 人体丢失时暂停状态机，不立即重置。

### 6.6 侧面机位深蹲评估

侧面机位负责：

1. 下蹲深度。
2. 膝关节角度。
3. 背部前倾。
4. 动作幅度。

推荐判断：

1. 膝关节角度越小，说明下蹲越深。
2. 髋部高度接近或低于膝盖高度，说明深度较好。
3. 肩到髋的连线相对竖直方向夹角过大，说明背部前倾明显。
4. 如果最低点膝角过大，提示“下蹲深度不足”。

结果输出：

```text
depthLevel: GOOD / SHALLOW / TOO_DEEP / UNKNOWN
kneeAngle: Float
trunkAngle: Float
sideProblems: List<String>
sideScore: Float
```

### 6.7 正面机位深蹲评估

正面机位负责：

1. 膝盖内扣。
2. 左右对称性。
3. 横向偏移。

推荐判断：

1. 计算左右膝盖相对左右脚踝的位置。
2. 如果膝盖明显向身体中心线移动，提示“膝盖内扣”。
3. 计算左右膝角、左右髋膝踝轨迹差异，判断左右不对称。
4. 只在人体正面入镜且关键点置信度足够时做判断。

结果输出：

```text
kneeValgus: Boolean
symmetryLevel: GOOD / NORMAL / POOR / UNKNOWN
frontProblems: List<String>
frontScore: Float
```

### 6.8 深蹲综合评分

评分先简单后复杂，第一版不追求医学级准确。

建议初始分 100 分，根据问题扣分：

```text
下蹲深度不足：-15
膝盖内扣：-20
背部前倾明显：-15
左右不对称：-10
人体识别不稳定：-10
动作节奏异常：-5
```

分数范围限制在 0 到 100。

建议输出：

```text
score: Float
postureLevel: EXCELLENT / GOOD / NEEDS_ATTENTION / POOR
problemType: String
suggestion: String
```

建议文案：

1. 下蹲深度不足：`适当增加下蹲幅度，让髋部接近膝盖高度。`
2. 膝盖内扣：`下蹲时保持膝盖朝向脚尖，避免向身体中线内扣。`
3. 背部前倾：`收紧核心，保持胸部打开，减少躯干过度前倾。`
4. 左右不对称：`注意左右脚均匀发力，保持身体中心稳定。`

### 6.9 开合跳计数

开合跳只做基础计数。

状态：

```text
CLOSED
OPENING
OPEN
CLOSING
```

判断特征：

1. 双脚脚踝横向距离。
2. 左右手腕高度。
3. 手腕是否高于肩膀。
4. 脚踝距离是否明显大于肩宽或髋宽。

计数规则：

1. 初始 CLOSED。
2. 手脚逐渐打开，进入 OPENING。
3. 手高于肩、脚距变大，进入 OPEN。
4. 手脚回收，进入 CLOSING。
5. 回到 CLOSED，计数加一。

输出：

```text
totalCount: Int
durationMs: Long
averageTempo: Float
lostFrameCount: Int
```

## 7. Nearby 多设备通信流程

### 7.1 角色

系统角色：

```text
MASTER
FRONT_CAMERA
SIDE_CAMERA
BACKUP_CAMERA
```

主控端负责：

1. 创建房间。
2. 开启 advertising。
3. 接受节点连接。
4. 分配机位角色。
5. 下发倒计时和训练指令。
6. 汇总节点分析结果。
7. 保存最终训练记录。

节点端负责：

1. 发现主控端。
2. 请求连接。
3. 接收角色。
4. 开启摄像头采集。
5. 本地姿态识别。
6. 上传关键点或分析结果。

### 7.2 连接策略

使用 Nearby Connections 的 `Strategy.P2P_STAR`。

主控端为中心节点，正面机位和侧面机位作为外围节点。这样最符合“一台主控手机连接多个摄像头节点”的项目结构。

### 7.3 通信消息格式

所有消息统一使用 JSON 字符串，通过 `Payload.fromBytes()` 发送。

通用字段：

```json
{
  "type": "MESSAGE_TYPE",
  "sessionId": 1001,
  "senderEndpointId": "endpoint_x",
  "timestampMs": 1710000000000,
  "payload": {}
}
```

推荐消息类型：

```text
JOIN_REQUEST
JOIN_ACCEPTED
DEVICE_STATUS
ASSIGN_ROLE
START_COUNTDOWN
START_ANALYSIS
POSE_FRAME
ANALYSIS_SUMMARY
END_TRAINING
HEARTBEAT
ERROR
```

### 7.4 角色分配消息

示例：

```json
{
  "type": "ASSIGN_ROLE",
  "sessionId": 1001,
  "timestampMs": 1710000000000,
  "payload": {
    "role": "FRONT_CAMERA",
    "actionType": "SQUAT"
  }
}
```

节点收到后：

1. 保存当前角色。
2. 更新 UI 显示。
3. 切换对应分析逻辑。

### 7.5 节点状态消息

示例：

```json
{
  "type": "DEVICE_STATUS",
  "sessionId": 1001,
  "timestampMs": 1710000000000,
  "payload": {
    "deviceName": "Xiaomi Phone",
    "batteryLevel": 82,
    "networkDelayMs": 38,
    "isOnline": true,
    "role": "FRONT_CAMERA"
  }
}
```

主控端收到后更新 DeviceGroupActivity。

### 7.6 姿态数据上传策略

不要无脑上传每一帧完整图片。推荐三种模式：

1. MVP 模式：节点本地分析后，只上传 `ANALYSIS_SUMMARY`。
2. 调试模式：节点上传抽样 `POSE_FRAME` JSON。
3. 报告模式：节点只在问题动作时上传关键帧路径或压缩图。

MVP 优先使用节点本地分析并上传摘要，减少 Nearby 通信压力。

### 7.7 同步倒计时

流程：

1. 主控端点击“开始同步倒计时”。
2. 主控端发送 `START_COUNTDOWN`，payload 中包含 `countdownSeconds: 3`。
3. 所有节点显示倒计时。
4. 倒计时结束后主控端发送 `START_ANALYSIS`。
5. 节点进入采集和姿态识别状态。

验收时要强调：多台手机能一起进入训练状态，这是多设备协作的核心亮点。

## 8. 独立分类器训练流程

### 8.1 模型训练定位

分类器训练是独立加分模块，不是 APP 主流程的前置条件。

不要训练人体检测模型，不要训练姿态关键点模型。ML Kit 已经完成关键点检测。要训练的是动作分类器，它的输入是连续若干帧人体关键点特征，输出是动作类型。

目标输出：

```text
SQUAT
JUMPING_JACK
UNKNOWN
```

可选扩展：

```text
STANDING
SQUAT_DOWN
SQUAT_UP
JUMPING_JACK_OPEN
JUMPING_JACK_CLOSED
```

### 8.2 独立目录

建议在项目根目录创建独立训练目录：

```text
model_training/
├── data/
│   ├── raw/
│   ├── labeled/
│   └── processed/
├── notebooks/
├── scripts/
├── models/
├── exports/
└── README.md
```

注意：创建该目录是后期模型训练阶段才需要做的事情。APP 主体开发阶段不需要创建。

### 8.3 数据来源

数据优先来自 APP 导出的关键点 JSON/CSV，而不是视频本身。

采集方式：

1. 使用 APP 的单机 CameraX + ML Kit 功能。
2. 录制深蹲、开合跳、站立、非目标动作。
3. 每一帧导出时间戳、关键点坐标、置信度、机位角色。
4. 保存为 JSON 或 CSV。
5. 在训练目录中进行标注和处理。

每条样本至少包含：

```text
sampleId
userId 或 subjectId
actionType
cameraRole
timestampMs
landmarks
confidence
stageLabel 可选
```

### 8.4 标注规范

动作标签：

```text
SQUAT
JUMPING_JACK
UNKNOWN
```

机位标签：

```text
FRONT_CAMERA
SIDE_CAMERA
SINGLE_CAMERA
```

阶段标签可选：

```text
STANDING
DESCENDING
BOTTOM
RISING
OPEN
CLOSED
```

标注原则：

1. 标签宁可少而准，不要大量错误标签。
2. 人体缺失、关键点混乱的片段标为 UNKNOWN 或直接丢弃。
3. 深蹲样本要包含站立、下蹲、起身完整过程。
4. 开合跳样本要包含打开和合拢完整周期。
5. 不同人、不同距离、不同光照至少各采一点，避免模型只记住单一场景。

### 8.5 特征工程

不要直接把原始像素图送进分类器。分类器使用关键点特征。

推荐特征：

1. 归一化后的关键点坐标。
2. 膝关节角度。
3. 髋关节角度。
4. 躯干倾角。
5. 双脚距离与肩宽比例。
6. 双手高度与肩膀高度关系。
7. 左右膝盖相对脚踝偏移。
8. 连续帧角度变化。
9. 连续帧髋部高度变化。

时序窗口：

```text
windowSize: 16 到 32 帧
stride: 4 到 8 帧
```

每个窗口输出一个动作类别。

### 8.6 推荐模型路线

优先级从简单到复杂：

1. 规则分类器：不用训练，直接用阈值判断，作为 APP fallback。
2. k-NN：数据少时容易实现，可解释，适合验证特征。
3. RandomForest：对手工特征友好，训练快，但 Android 端部署不如 TFLite 直接。
4. 小型 MLP：输入特征向量，输出动作类别，容易转换为 TFLite。
5. LSTM 或 1D CNN：适合时序动作识别，但课程项目中只有时间充裕时再做。

本文档推荐最终接入形态为：**关键点特征 + 小型 MLP + TFLite**。

### 8.7 训练产物

模型训练完成后导出：

```text
action_classifier.tflite
labels.txt
model_meta.json
```

`labels.txt` 示例：

```text
SQUAT
JUMPING_JACK
UNKNOWN
```

`model_meta.json` 示例：

```json
{
  "modelName": "zhizijing_action_classifier",
  "version": "1.0",
  "inputType": "pose_landmark_sequence",
  "windowSize": 24,
  "featureCount": 64,
  "labels": ["SQUAT", "JUMPING_JACK", "UNKNOWN"],
  "confidenceThreshold": 0.7
}
```

Android 接入时放到：

```text
app/src/main/assets/action_classifier.tflite
app/src/main/assets/labels.txt
app/src/main/assets/model_meta.json
```

### 8.8 训练验收标准

分类器独立验收：

1. 能读取 APP 导出的关键点数据。
2. 能完成训练集、验证集划分。
3. 能输出准确率、混淆矩阵。
4. 能导出 `.tflite`。
5. 能用同一条样本离线推理并输出动作类别和置信度。
6. 即使模型效果一般，也不能影响 APP 主流程演示。

## 9. 分类器与 APP 对接流程

### 9.1 统一接口

在 `pose.classifier` 中定义统一接口。

接口含义：

```text
ActionClassifier
输入：一段 PoseFrame 窗口
输出：动作类别、置信度、窗口开始时间、窗口结束时间
```

推荐输出结构：

```text
ActionClassificationResult
actionType: String
confidence: Float
windowStartMs: Long
windowEndMs: Long
source: RULE_BASED / TFLITE
```

分类器必须输出：

```text
actionType
confidence
windowStartMs
windowEndMs
```

### 9.2 两种实现

第一种：`RuleBasedActionClassifier`

用途：

1. MVP 默认使用。
2. 不依赖模型文件。
3. 用关键点规则判断深蹲、开合跳、未知动作。
4. TFLite 模型加载失败时 fallback。

第二种：`TFLiteActionClassifier`

用途：

1. 加载 `action_classifier.tflite`。
2. 读取 `labels.txt`。
3. 将 PoseFrame 窗口转换成模型输入张量。
4. 输出动作类别和置信度。

### 9.3 接入原则

分类器只负责动作类型识别，不负责动作评分。

正确流程：

```text
CameraX 帧
-> ML Kit Pose Detection
-> PoseFrame
-> ActionClassifier 判断动作类型
-> 根据 actionType 选择 SquatAnalyzer 或 JumpingJackAnalyzer
-> 规则模块完成计数和评分
-> UI 展示结果
```

错误流程：

```text
分类器直接输出深蹲评分
```

不要这样做。评分要保留在规则模块中，方便解释和调试。

### 9.4 模型接入后的 UI 变化

训练首页可以增加“自动识别动作”模式。

实时评估页面显示：

```text
识别动作：深蹲
识别置信度：0.86
分类器来源：TFLite
```

当置信度不足时：

```text
动作识别不稳定，请调整站位或重新开始。
```

### 9.5 模型失败处理

必须处理：

1. 模型文件不存在。
2. labels 文件不存在。
3. 输入维度不匹配。
4. 推理异常。
5. 置信度过低。

处理策略：

1. 自动切换到 `RuleBasedActionClassifier`。
2. 页面提示“当前使用规则识别模式”。
3. 不影响深蹲计数和评分。

## 10. 报告导出流程

### 10.1 JSON 导出

JSON 用于模型训练和验收展示。

训练摘要 JSON：

```json
{
  "sessionId": 1001,
  "actionType": "SQUAT",
  "startTime": 1710000000000,
  "endTime": 1710000030000,
  "deviceCount": 2,
  "totalCount": 30,
  "averageScore": 86.0,
  "mainProblems": ["SHALLOW_DEPTH", "KNEE_VALGUS"],
  "results": []
}
```

姿态样本 JSON：

```json
{
  "sessionId": 1001,
  "cameraRole": "SIDE_CAMERA",
  "frames": []
}
```

注意：用于模型训练的姿态 JSON 可以只在调试开关打开时导出，避免普通使用时产生大量文件。

### 10.2 PDF 导出

PDF 用于课程验收。

PDF 内容建议：

1. 标题：智姿镜训练报告。
2. 训练动作。
3. 训练时间。
4. 参与设备数。
5. 总次数。
6. 平均评分。
7. 主要问题。
8. 改进建议。
9. 正面关键帧。
10. 侧面关键帧。

使用 Android 原生 `PdfDocument` 即可，不需要引入复杂 PDF 库。

## 11. 测试与验收清单

### 11.1 单机测试

必须通过：

1. APP 能启动。
2. 能注册和登录。
3. 首页能跳转。
4. CameraX 能打开摄像头。
5. ML Kit 能检测人体关键点。
6. 画面上能显示骨架。
7. 深蹲能计数。
8. 深蹲问题提示能出现。
9. 训练结束能保存结果。
10. 历史记录能看到结果。

### 11.2 多机测试

至少准备两台安卓手机。

必须通过：

1. 主控端创建房间。
2. 节点端加入房间。
3. 主控端显示节点设备。
4. 主控端分配正面机位或侧面机位。
5. 节点端显示自己的机位角色。
6. 主控端发送倒计时。
7. 节点端同步进入训练。
8. 主控端能看到节点分析状态。
9. 结束训练后主控端生成结果。

### 11.3 深蹲验收

必须能演示：

1. 正常深蹲计数。
2. 故意下蹲太浅，提示下蹲深度不足。
3. 故意膝盖内扣，提示膝盖内扣。
4. 故意身体前倾，提示背部前倾。
5. 训练结果页显示总次数、平均评分、主要问题、改进建议。

### 11.4 开合跳验收

加分验收：

1. 正常开合跳计数。
2. 显示训练时长。
3. 显示平均节奏。
4. 人体离开画面时提示识别中断。

### 11.5 分类器验收

加分验收：

1. APP 主流程在没有分类器时仍能演示。
2. 接入分类器后能显示识别动作类型。
3. 分类器能输出置信度。
4. 模型异常时能 fallback 到规则识别。
5. 分类器只负责动作类型识别，不直接负责评分。

## 12. 推荐开发里程碑

### 第 1 阶段：基础项目

目标：

1. Android 项目创建完成。
2. Kotlin + XML + ViewBinding 可用。
3. 基础主题、权限、导航结构完成。

完成后截图：

1. 启动页或登录页。
2. 首页。

### 第 2 阶段：本地数据

目标：

1. User、TrainingSession、ActionResult 基础表完成。
2. 登录状态使用 DataStore 保存。
3. 历史记录能显示假数据或真实数据。

完成后截图：

1. 登录页。
2. 首页最近训练。
3. 历史记录页。

### 第 3 阶段：单机姿态识别

目标：

1. CameraX Preview。
2. ImageAnalysis。
3. ML Kit Pose Detection。
4. 骨架绘制。

完成后截图：

1. 摄像头预览。
2. 人体骨架覆盖。

### 第 4 阶段：深蹲评估

目标：

1. 深蹲状态机。
2. 深蹲计数。
3. 角度计算。
4. 问题判断。
5. 评分和建议。

完成后截图：

1. 实时评估页面。
2. 问题提示。
3. 结果页。

### 第 5 阶段：多设备协作

目标：

1. Nearby 创建房间。
2. Nearby 加入房间。
3. 设备列表。
4. 机位分配。
5. 同步倒计时。

完成后截图：

1. 房间码和二维码。
2. 设备组队页面。
3. 节点角色页面。

### 第 6 阶段：结果和导出

目标：

1. 训练结果保存。
2. 历史详情。
3. JSON 导出。
4. PDF 导出。

完成后截图：

1. 训练结果。
2. 历史详情。
3. PDF 报告。

### 第 7 阶段：分类器加分

目标：

1. 从 APP 导出关键点数据。
2. 独立训练分类器。
3. 导出 TFLite。
4. APP 接入 TFLite。
5. 保留规则 fallback。

完成后截图：

1. 模型训练结果。
2. APP 显示动作识别类型和置信度。

## 13. 给 AI 的阶段提示词模板

### 13.1 创建项目提示词

```text
请阅读 制作指南.md。当前任务是创建智姿镜 Android 原生项目基础结构。技术栈固定为 Kotlin + XML + ViewBinding + Activity/MVVM。请只完成项目结构、基础依赖、主题、权限声明、包结构和空页面，不要实现 CameraX、Nearby 或模型训练。完成后说明如何运行和验收。
```

### 13.2 登录和数据库提示词

```text
请阅读 制作指南.md。当前任务是实现登录注册、Room 用户表、训练记录基础表、DataStore 登录状态和首页最近训练展示。不要实现 CameraX、ML Kit、Nearby 和分类器。完成后给出真机验收步骤。
```

### 13.3 CameraX 和 ML Kit 提示词

```text
请阅读 制作指南.md。当前任务是实现单机 CameraX 预览、ImageAnalysis、ML Kit Pose Detection 和人体骨架 Overlay。请封装 PoseFrame，不要把 ML Kit 原始对象直接传到页面之外。不要实现多设备通信和模型训练。
```

### 13.4 深蹲评估提示词

```text
请阅读 制作指南.md。当前任务是基于 PoseFrame 实现深蹲状态机、角度计算、次数统计、下蹲深度、膝盖内扣、背部前倾、左右对称、评分和建议。先用规则实现，不要接入 TFLite 分类器。
```

### 13.5 Nearby 多设备提示词

```text
请阅读 制作指南.md。当前任务是实现 Nearby Connections 多设备组队。主控端创建房间并 advertising，节点端 discovery 并连接，使用 Strategy.P2P_STAR。实现设备列表、角色分配、状态上报、同步倒计时。不要训练模型。
```

### 13.6 报告导出提示词

```text
请阅读 制作指南.md。当前任务是实现训练结果保存、历史记录详情、JSON 导出和 PdfDocument PDF 报告导出。报告包含训练概览、次数、评分、主要问题、建议和关键帧。不要修改姿态识别算法。
```

### 13.7 分类器训练提示词

```text
请阅读 制作指南.md。当前任务是独立完成动作分类器训练流程。不要修改 Android APP 主体。请基于 APP 导出的关键点 JSON/CSV 设计数据处理、标注、特征提取、训练、评估和 TFLite 导出流程。分类器输出 actionType、confidence、windowStartMs、windowEndMs。
```

### 13.8 分类器接入提示词

```text
请阅读 制作指南.md。当前任务是把已训练好的 action_classifier.tflite 接入 Android APP。请新增 ActionClassifier 接口，保留 RuleBasedActionClassifier，实现 TFLiteActionClassifier。分类器只负责动作类型识别，评分仍由深蹲和开合跳规则模块完成。模型失败时必须 fallback 到规则识别。
```

## 14. 最终验收演示脚本

建议现场演示按以下顺序进行：

1. 打开 APP，展示登录和首页。
2. 点击创建训练房间，展示房间码和二维码。
3. 第二台手机加入房间。
4. 主控端展示设备列表、电量、延迟。
5. 分配一台为正面机位，一台为侧面机位。
6. 点击开始同步倒计时。
7. 节点手机进入摄像头预览并显示骨架。
8. 用户做 3 到 5 个深蹲。
9. 主控端展示次数、角度、评分和问题提示。
10. 故意做一个下蹲太浅或膝盖内扣的动作，展示问题识别。
11. 结束训练，进入结果页。
12. 展示历史记录。
13. 导出 PDF 或 JSON 报告。
14. 如果分类器已接入，展示自动识别动作类型和置信度。

## 15. 最终交付标准

项目最终交付至少包含：

1. 可运行 Android 项目。
2. 真机演示视频或截图。
3. 至少两台手机协同演示。
4. 深蹲实时计数和评分。
5. 训练结果保存和历史记录。
6. 关键页面截图。
7. 数据库设计说明。
8. 系统结构图。
9. 页面设计草图。
10. 如果完成模型训练，提供 `.tflite`、`labels.txt`、`model_meta.json` 和训练评估结果。

只要 MVP 稳定，多机位协作和深蹲评估能现场演示，本项目就已经具备较强的课程设计亮点。分类器模型是锦上添花，不要让它反过来拖慢主功能完成。
