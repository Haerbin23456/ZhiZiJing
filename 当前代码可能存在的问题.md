# 当前代码可能存在的问题

本文档根据以下材料整理：开题报告动作识别版 PDF、普通版 PDF、`智姿镜_Guideline文档必要内容说明.md`、`制作指南.md`、`PROJECT_CONTEXT.md` 以及当前 `ZhiZiJing` 工程源码。

## 0. 2026-05-30 本轮修复状态

已修复：

1. 构建环境：已加入 `android.overridePathCheck=true`，并写入本机 `local.properties` 的 `sdk.dir`，当前路径下可直接执行 `:app:assembleDebug`。
2. UI 技术路线：主界面已从 Compose 展示壳迁回 XML + ViewBinding。
3. 入口 Activity：Manifest 已指向 `com.example.zhizijing.ui.main.MainActivity`。
4. 单文件 Compose 展示壳：旧根包 `MainActivity.kt` 已移除，新的 `MainActivity` 只负责展示当前工程状态和待实现入口。
5. 静态假数据：主源码中已移除写死房间码、假设备、假评分、假历史记录等容易误导的演示数据。
6. 模板测试：`2 + 2 = 4` 模板测试已替换为项目状态检查测试。

仍未在本轮实现：

1. Room、DataStore、CameraX、ML Kit、Nearby、PDF/JSON、规则动作识别等功能仍按 `接下来的工作.md` 后续阶段开发。
2. `:app:testDebugUnitTest` 在当前中文路径下仍受 Gradle/JDK worker classpath 编码问题影响，会把路径写成乱码导致 JUnit 找不到测试类；测试类本身已编译通过，并已用同一编译产物通过 `JUnitCore` 手动验证。若要让 Gradle 单元测试任务稳定通过，建议后续把项目移动或映射到纯英文路径。

结论先说清楚：本轮修复后，当前代码已经从 Compose 展示壳迁回 XML + ViewBinding 工程基础，主页面只展示项目状态和待实现入口，不再伪装真实业务已经完成。它仍不是开题报告中要求的完整智姿镜 APP，Room、CameraX、ML Kit、Nearby、规则评估、历史记录和报告导出等真实功能还需要按后续计划继续开发。

下面第 1-12 节保留的是修复前的问题清单，已经解决的部分以第 0 节为准；仍未实现的业务功能继续按 `接下来的工作.md` 推进。

## 1. 当前代码已经完成的内容

1. 已经创建 Android 工程，项目目录为 `ZhiZiJing/`，包含 `app` 模块、Gradle Wrapper、基础资源文件和测试模板。
2. `MainActivity.kt` 可以编译通过，并使用 Jetpack Compose + Material3 搭建了一个单 Activity 页面壳。
3. 当前页面包含五个展示入口：训练、房间、节点、评估、历史。
4. 页面中已经用静态 UI 表达了项目核心概念，例如“创建房间”“CameraX + ML Kit 预览区域”“深蹲次数”“当前评分”“报告导出”等。
5. 当前工程在临时设置 `ANDROID_HOME=C:\Users\liuli\AppData\Local\Android\Sdk`，并临时传入 `-Pandroid.overridePathCheck=true` 后，执行 `:app:assembleDebug` 可以构建成功。

这些内容只能说明项目基础壳存在，不能算作已经实现开题报告中的真实功能。

## 2. 与开题报告功能点的对照

| 开题报告 / 指南要求 | 当前代码状态 | 判断 |
|---|---|---|
| 登录注册与自动登录 | 没有 `LoginActivity`、`RegisterActivity`、用户表、登录状态保存 | 未实现 |
| 训练首页展示真实最近训练、平均评分、累计次数 | 只有 Compose 静态卡片，数据写死 | 未实现 |
| 创建训练房间、生成房间码和二维码 | 只有写死的房间码 `839 204`，没有生成逻辑 | 未实现 |
| 节点加入房间 | 没有加入页面、扫码、输入房间码或 Nearby discovery | 未实现 |
| 多设备组队与机位分配 | 只有静态设备文字，没有真实设备列表、角色同步 | 未实现 |
| 同步倒计时 | 没有倒计时页面和指令分发 | 未实现 |
| CameraX 摄像头预览 | 只有“CameraX + ML Kit 预览区域”占位卡片 | 未实现 |
| CameraX ImageAnalysis 帧采集 | 未引入 CameraX 依赖，也没有分析管线 | 未实现 |
| ML Kit Pose Detection 姿态关键点识别 | 未引入 ML Kit 依赖，也没有 PoseDetector | 未实现 |
| 人体骨架 Overlay 绘制 | 没有自定义 View 或 Compose Canvas 绘制骨架 | 未实现 |
| 动作姿势识别入口 | 只有概念文案，没有识别逻辑 | 未实现 |
| 规则判断深蹲 / 开合跳 | 没有 `RuleBasedActionClassifier`，没有状态机 | 未实现 |
| 深蹲计数 | 页面显示静态 `12`，没有基于关键点计数 | 未实现 |
| 深蹲评分和问题提示 | 页面显示静态评分和建议，没有算法 | 未实现 |
| 开合跳计数 | 只有历史里写死一条开合跳记录 | 未实现 |
| Room 保存训练记录 | 未引入 Room，未定义 Entity/DAO/Database | 未实现 |
| DataStore 保存登录状态和配置 | 未引入 DataStore，未定义配置项 | 未实现 |
| 历史记录查看 | 只有静态列表，不读数据库 | 未实现 |
| JSON / PDF 报告导出 | 只有文案，没有导出实现 | 未实现 |
| TFLite 动作分类模型 | 当前阶段按你的要求暂不做，后续扩展 | 暂缓实现 |

## 3. 构建与环境问题

1. 项目路径包含中文：`C:\Users\liuli\Desktop\大三下课程\移动应用开发\课设\ZhiZiJing`。Android Gradle Plugin 在 Windows 上会报错，提示项目路径包含非 ASCII 字符。
2. 当前工程没有 `local.properties`，命令行构建时找不到 Android SDK。虽然本机存在 SDK：`C:\Users\liuli\AppData\Local\Android\Sdk`，但 Gradle 不会自动知道这个路径。
3. 临时构建可行命令为：先设置 `ANDROID_HOME`，再执行 `gradlew :app:assembleDebug -Pandroid.overridePathCheck=true`。但这只是临时绕过，不是项目级解决方案。
4. 如果后续在 Android Studio 中打开项目，IDE 可能自动生成 `local.properties`，但命令行和其他电脑仍可能遇到同样问题。

建议后续处理方式：

1. 最稳妥方式是把项目复制到不含中文和空格的路径，例如 `C:\AndroidProjects\ZhiZiJing`。
2. 如果必须保留当前路径，可以在 `gradle.properties` 中加入 `android.overridePathCheck=true`，但这属于绕过 AGP 检查。
3. 在本机开发时需要保证 `local.properties` 中存在正确的 `sdk.dir`，或系统环境变量配置了 `ANDROID_HOME`。

## 4. 架构与技术路线问题

1. `制作指南.md` 明确要求项目默认技术路线为 `Kotlin + XML + ViewBinding + Activity/MVVM`，但当前代码使用的是 `Compose + Material3 + 单 Activity`。
2. 当前所有 UI、页面状态、跳转逻辑、静态数据都集中在 `MainActivity.kt` 一个文件中，后续接入 CameraX、Room、Nearby 后会非常难维护。
3. 当前没有按文档推荐建立 `ui`、`data`、`domain`、`pose`、`nearby`、`report`、`utils` 等包结构。
4. 当前没有 ViewModel、Repository、UseCase 或数据层抽象，页面和业务没有边界。
5. 当前存在 `activity_main.xml`，但 `MainActivity` 实际没有使用该 XML，因此它只是空布局文件。
6. 如果继续保留 Compose，需要同步修改制作指南和开题报告中的技术路线；但你已经确认后续应转回 XML/ViewBinding，所以当前 Compose 壳应作为临时参考，而不是最终结构。

## 5. 页面与交互问题

1. 当前首页没有登录前置流程，打开 APP 直接进入主页面，不符合“登录成功后进入训练首页”的要求。
2. 当前页面切换只是 Compose 内部 tab 切换，不是真正的 Activity 流程，也没有参数传递和生命周期边界。
3. “创建房间”“直接评估”“查看节点画面”“开始评估”“结束并查看历史”等按钮没有触发真实业务，只是切换展示页。
4. 房间码、节点设备、电量、延迟、深蹲次数、评分、膝关节角和历史记录全部是写死数据。
5. 当前没有空状态、错误状态、权限拒绝状态、识别失败状态、设备断连状态。
6. 当前没有训练准备页面、同步倒计时页面、结果详情页面、历史详情页面。

## 6. 依赖和权限问题

当前 `app/build.gradle.kts` 只引入了基础 AndroidX、AppCompat、Material、ConstraintLayout 和 Compose 相关依赖，缺少以下关键依赖：

1. Room：用户、训练记录、设备节点、动作结果、导出记录无法持久化。
2. DataStore Preferences：无法保存登录状态、阈值配置、默认动作等轻量配置。
3. CameraX：无法进行摄像头预览、帧采集和视频保存。
4. ML Kit Pose Detection：无法提取人体关键点。
5. Nearby Connections：无法实现多手机连接、角色分配和训练指令同步。
6. Gson 或 Moshi：无法统一 Nearby JSON 消息和报告 JSON。
7. ZXing 或 ML Kit Barcode Scanning：无法生成或扫描房间二维码。
8. TensorFlow Lite：后期无法接入动作分类模型。

当前 `AndroidManifest.xml` 也缺少后续必需权限，包括但不限于：

1. `android.permission.CAMERA`
2. `android.permission.RECORD_AUDIO`，如果后续保存视频声音才需要
3. `android.permission.ACCESS_FINE_LOCATION`，Nearby 在部分 Android 版本需要
4. Android 12 及以上 Nearby/蓝牙相关权限，例如 `BLUETOOTH_SCAN`、`BLUETOOTH_ADVERTISE`、`BLUETOOTH_CONNECT`
5. Android 13 及以上媒体读取权限，如果后续访问公共媒体文件才需要

## 7. 数据层问题

1. 没有 `User` 表，无法注册、登录、区分用户训练记录。
2. 没有 `TrainingSession` 表，无法保存一次训练的开始时间、结束时间、动作类型、总次数、平均评分。
3. 没有 `DeviceNode` 表，无法记录参与训练的主控端和摄像头节点。
4. 没有 `PoseFrame` 表或模型，无法保存关键帧和问题帧。
5. 没有 `ActionResult` 表，无法保存每一次深蹲或开合跳的分析结果。
6. 没有 `ExportRecord` 表，无法记录 PDF/JSON 导出历史。
7. 没有 Repository，页面后续会直接操作数据库，容易造成逻辑混乱。

## 8. 姿态识别与动作分析问题

1. 当前没有 `PoseFrame` 和 `LandmarkPoint` 标准数据结构。
2. 当前没有把 ML Kit 输出转换为项目内部统一关键点格式的适配层。
3. 当前没有低置信度过滤规则，例如关键点置信度不足时暂停评分。
4. 当前没有三点夹角、两点距离、身体中心线、归一化距离等基础计算工具。
5. 当前没有深蹲状态机，无法从连续帧中识别 `STANDING -> DESCENDING -> SQUATTING -> RISING -> STANDING`。
6. 当前没有开合跳状态机，无法识别手脚从合拢到打开再回到合拢的完整周期。
7. 当前没有评分扣分规则，也没有问题类型和建议文案的统一输出。
8. 当前没有规则动作分类器。根据你的最新要求，第一版应该先用规则判断深蹲和开合跳，而不是一开始训练复杂模型。

## 9. 多设备通信问题

1. 当前没有 Nearby advertising/discovery。
2. 当前没有主控端和节点端角色区分。
3. 当前没有统一 Nearby 消息结构。
4. 当前没有 `JOIN_REQUEST`、`ASSIGN_ROLE`、`START_COUNTDOWN`、`START_ANALYSIS`、`ANALYSIS_SUMMARY`、`END_TRAINING` 等消息处理。
5. 当前没有心跳、在线状态、电量、延迟上报。
6. 当前没有节点断连或识别失败后的降级提示。

## 10. 报告与历史问题

1. 当前历史记录只是静态文本，不会保存训练结果。
2. 当前没有历史详情页面，无法查看某次训练的完整动作分析。
3. 当前没有本地文件目录约定的实现，例如 `files/training/{sessionId}/frames/` 和 `reports/`。
4. 当前没有 JSON 导出，后续动作分类模型也无法复用 APP 导出的关键点数据。
5. 当前没有 `PdfDocument` 导出 PDF 报告。
6. 当前没有关键帧截图保存能力。

## 11. 测试问题

1. 本地单元测试仍是 Android Studio 模板的 `2 + 2 = 4`。
2. 仪器测试也只是检查包名，没有覆盖任何业务。
3. 缺少角度计算、深蹲状态机、开合跳状态机、评分规则的单元测试。
4. 缺少 Room DAO 测试。
5. 缺少 CameraX 权限和 ML Kit 识别的真机验收清单。
6. 缺少 Nearby 两台手机联调验收清单。

## 12. 优先级判断

当前最紧急的问题不是训练模型，而是 APP 主流程还没有真实闭环。建议严格按以下优先级处理：

1. P0：转回 XML/ViewBinding 架构，完成登录、首页、页面跳转、Room、DataStore。
2. P0：先用 Fake 数据跑通一次训练流程，保证结果页和历史记录能保存。
3. P0：接入 CameraX + ML Kit，生成真实 PoseFrame 和骨架 Overlay。
4. P0：用规则完成深蹲计数、评分和问题提示。
5. P1：实现开合跳基础计数。
6. P1：实现 Nearby 多设备组队和机位分配。
7. P1：实现 JSON/PDF 报告导出。
8. P2：主功能稳定后，再训练并接入 10 种以上动作的 TFLite 分类器，同时保留规则识别 fallback。
