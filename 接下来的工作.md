# 接下来的工作

本文档按照“先完成可演示闭环，再接入真实能力，最后扩展模型”的思路安排。根据你的要求，第一版不先训练复杂动作识别模型，而是先用规则判断深蹲和开合跳，把 APP 的其他核心功能全部做完；等主流程稳定后，再扩展 TFLite 动作分类模型，让它识别十种以上动作。

## 总体目标

最终 APP 要能完成以下演示流程：

1. 用户注册 / 登录。
2. 主控端创建训练房间。
3. 摄像头节点加入房间。
4. 主控端给节点分配正面机位或侧面机位。
5. 节点端打开 CameraX 预览并显示人体骨架。
6. 主控端发送同步倒计时和开始训练指令。
7. 系统用规则识别深蹲或开合跳。
8. 深蹲能计数、评分并提示问题；开合跳能基础计数。
9. 训练结束后进入结果页。
10. 训练记录保存到 Room，历史记录可查看。
11. 可导出 JSON 或 PDF 报告。
12. 后期接入 TFLite 动作分类器，扩展到十种以上动作。

## 第 1 步：修正工程基础和技术路线

目标：把当前 Compose 单 Activity 展示壳调整为符合文档的 `Kotlin + XML + ViewBinding + Activity/MVVM` 基础结构。

需要做：

1. 在 `app/build.gradle.kts` 中启用 `viewBinding = true`。
2. 逐步移除 Compose 作为主 UI 方案，后续页面用 XML 布局和 ViewBinding 实现。
3. 保留当前 Compose 页面作为临时参考，不再继续在 `MainActivity.kt` 里堆业务。
4. 建立推荐包结构：
   - `ui/auth`
   - `ui/main`
   - `ui/room`
   - `ui/device`
   - `ui/prepare`
   - `ui/camera`
   - `ui/analysis`
   - `ui/result`
   - `ui/history`
   - `data/entity`
   - `data/dao`
   - `data/local`
   - `data/datastore`
   - `data/repository`
   - `domain/model`
   - `domain/rule`
   - `pose/model`
   - `pose/detector`
   - `pose/overlay`
   - `pose/feature`
   - `pose/classifier`
   - `nearby/connection`
   - `nearby/message`
   - `report/json`
   - `report/pdf`
   - `utils`
5. 处理构建环境：
   - 推荐把项目移动到不含中文的路径，例如 `C:\AndroidProjects\ZhiZiJing`。
   - 或在本机 `local.properties` 配置 `sdk.dir`。
   - 如果继续放在中文路径，需要在 Gradle 参数或 `gradle.properties` 中处理 `android.overridePathCheck=true`。

完成标准：

1. `assembleDebug` 能正常通过。
2. `MainActivity` 不再承担所有页面和业务。
3. 项目包结构已经为后续功能预留清晰位置。

## 第 2 步：搭建页面骨架和跳转流程

目标：先把开题报告要求的页面都建出来，并能按流程跳转。

需要实现的页面：

1. `LoginActivity`：登录页面。
2. `RegisterActivity`：注册页面。
3. `MainActivity`：训练首页。
4. `RoomActivity`：创建训练房间。
5. `JoinRoomActivity`：加入训练房间。
6. `DeviceGroupActivity`：设备组队和机位分配。
7. `PrepareActivity`：训练准备和同步倒计时。
8. `CameraNodeActivity`：摄像头节点采集页面。
9. `ActionAnalysisActivity`：动作实时评估页面，兼容深蹲和开合跳。
10. `ResultActivity`：训练结果页面。
11. `HistoryActivity`：历史记录页面。
12. `HistoryDetailActivity`：历史详情和报告导出入口。

建议先做简单 XML 页面，不追求复杂视觉效果。每个页面必须有清晰的标题、核心信息区域和下一步按钮。

完成标准：

1. APP 启动先进入登录页。
2. 登录成功进入首页。
3. 首页可以进入创建房间、加入房间、历史记录。
4. 创建房间后可以进入设备组队页。
5. 设备组队页可以进入准备页。
6. 准备页倒计时结束后可以进入实时评估页。
7. 实时评估结束后可以进入结果页。
8. 结果页可以返回首页或进入历史记录。

## 第 3 步：定义统一枚举、模型和接口

目标：先把后续模块都要共用的数据格式固定下来，避免每个页面各写一套字符串。

需要定义的枚举：

1. `DeviceRole`
   - `HOST`
   - `FRONT_CAMERA`
   - `SIDE_CAMERA`
   - `BACKUP_CAMERA`
   - `UNKNOWN`
2. `ActionType`
   - `SQUAT`
   - `JUMPING_JACK`
   - `STANDING`
   - `UNKNOWN`
3. `TrainingState`
   - `IDLE`
   - `PREPARING`
   - `ANALYZING`
   - `FINISHED`
   - `ERROR`
4. `ProblemType`
   - `NONE`
   - `SQUAT_DEPTH_NOT_ENOUGH`
   - `KNEE_INWARD`
   - `BACK_LEAN_TOO_MUCH`
   - `ASYMMETRY`
   - `LOW_CONFIDENCE`
   - `RHYTHM_ABNORMAL`
5. `ClassifierSource`
   - `RULE_BASED`
   - `TFLITE`

需要定义的核心模型：

1. `LandmarkPoint`
   - `name`
   - `x`
   - `y`
   - `z`
   - `confidence`
2. `PoseFrame`
   - `sessionId`
   - `nodeId`
   - `timestampMs`
   - `cameraRole`
   - `landmarks`
   - `overallConfidence`
   - `imageWidth`
   - `imageHeight`
3. `ActionClassificationResult`
   - `actionType`
   - `confidence`
   - `windowStartMs`
   - `windowEndMs`
   - `source`
4. `SquatAnalysisResult`
   - `totalCount`
   - `currentStage`
   - `score`
   - `kneeAngle`
   - `trunkAngle`
   - `depthLevel`
   - `problemType`
   - `suggestion`
5. `JumpingJackResult`
   - `totalCount`
   - `durationMs`
   - `averageTempo`
   - `lostFrameCount`

需要定义的接口：

1. `ActionClassifier`
   - 输入连续 `PoseFrame`
   - 输出 `ActionClassificationResult`
2. `PoseDetectorAdapter`
   - 把 ML Kit 的输出转换成项目内部 `PoseFrame`
3. `SquatAnalyzer`
   - 输入 `PoseFrame`
   - 输出深蹲计数、阶段、评分和问题
4. `JumpingJackAnalyzer`
   - 输入 `PoseFrame`
   - 输出开合跳次数、节奏和识别状态
5. `NearbyMessageCodec`
   - 负责 Nearby JSON 消息编码和解码
6. `ReportExporter`
   - 负责 JSON/PDF 报告导出

完成标准：

1. 所有模块都引用统一枚举，不再到处写散乱字符串。
2. 姿态识别、动作识别、评分、数据库、Nearby 都能使用同一套模型。

## 第 4 步：接入 Room 和 DataStore

目标：先完成本地数据能力，让登录、历史记录、训练结果保存都有基础。

需要添加依赖：

1. Room runtime。
2. Room KTX。
3. Room compiler / KSP。
4. DataStore Preferences。

需要实现 Room 表：

1. `UserEntity`
   - `userId`
   - `username`
   - `passwordHash`
   - `nickname`
   - `createdAt`
   - `lastLoginAt`
2. `TrainingSessionEntity`
   - `sessionId`
   - `userId`
   - `actionType`
   - `startTime`
   - `endTime`
   - `deviceCount`
   - `totalCount`
   - `averageScore`
   - `mainProblems`
   - `reportPath`
   - `status`
3. `DeviceNodeEntity`
   - `nodeId`
   - `sessionId`
   - `deviceName`
   - `endpointId`
   - `role`
   - `batteryLevel`
   - `networkDelayMs`
   - `isOnline`
   - `lastHeartbeatAt`
4. `PoseFrameEntity`
   - `frameId`
   - `sessionId`
   - `nodeId`
   - `timestampMs`
   - `cameraRole`
   - `landmarksJson`
   - `confidence`
   - `frameImagePath`
   - `frameType`
5. `ActionResultEntity`
   - `resultId`
   - `sessionId`
   - `actionIndex`
   - `actionType`
   - `startTimeMs`
   - `endTimeMs`
   - `score`
   - `kneeAngle`
   - `trunkAngle`
   - `depthLevel`
   - `problemType`
   - `suggestion`
   - `keyFramePath`
6. `ExportRecordEntity`
   - `exportId`
   - `sessionId`
   - `exportType`
   - `filePath`
   - `createdAt`
   - `fileSize`

需要实现 DataStore 配置：

1. `isLoggedIn`
2. `lastUserId`
3. `defaultActionType`
4. `lastRoomCode`
5. `minPoseConfidence`
6. `squatDepthThreshold`
7. `backLeanThreshold`
8. `actionRecognitionThreshold`
9. `showSkeletonOverlay`
10. `saveVideoEnabled`
11. `defaultExportDir`

完成标准：

1. 能注册新用户。
2. 能登录已有用户。
3. 重启 APP 后能自动登录。
4. 首页能从数据库读取最近训练。
5. 历史记录页能读取训练列表。

## 第 5 步：先用 Fake 数据跑通训练闭环

目标：在没有 CameraX、ML Kit、Nearby 的情况下，先证明 APP 主流程完整。

需要做：

1. 在创建房间页生成一个本地假房间码。
2. 在设备组队页显示模拟主控端、正面机位、侧面机位。
3. 在准备页实现真实 3 秒倒计时。
4. 在实时评估页用定时器模拟深蹲数据变化：
   - 次数逐渐增加
   - 当前阶段变化
   - 分数变化
   - 随机出现下蹲深度不足、膝盖内扣、背部前倾提示
5. 点击结束训练后生成 `TrainingSessionEntity` 和多条 `ActionResultEntity`。
6. 结果页读取刚保存的数据。
7. 历史记录页能看到新训练记录。

完成标准：

1. 不接摄像头也能完整演示“开始训练 -> 实时评估 -> 结束训练 -> 结果页 -> 历史记录”。
2. 训练记录是真实保存到 Room 的，不是写死在页面上。
3. 后续接入真实识别时，只需要替换数据来源。

## 第 6 步：接入 CameraX 摄像头采集

目标：让节点端能打开摄像头并实时采集帧。

需要添加依赖：

1. `camera-core`
2. `camera-camera2`
3. `camera-lifecycle`
4. `camera-view`
5. 可选：`camera-video`

需要添加权限：

1. `android.permission.CAMERA`
2. 如果保存带声音视频，再添加 `RECORD_AUDIO`

需要实现：

1. `CameraNodeActivity` 申请相机权限。
2. 使用 `PreviewView` 显示实时画面。
3. 使用 `ImageAnalysis` 获取帧。
4. 控制分析帧率，避免手机过热和卡顿。
5. 生命周期结束时释放相机。

完成标准：

1. 真机能打开摄像头。
2. 摄像头预览不卡死。
3. 权限拒绝时页面有提示。
4. 能把图像帧交给下一步 ML Kit 处理。

## 第 7 步：接入 ML Kit Pose Detection 和骨架 Overlay

目标：从摄像头画面中识别人体关键点，并显示骨架。

需要添加依赖：

1. ML Kit Pose Detection。
2. 如需要更高精度，可使用 accurate 版本。

需要实现：

1. `PoseDetectorAdapter` 创建 `PoseDetector`。
2. 使用 `InputImage.fromMediaImage()` 转换 CameraX 图像帧。
3. 读取常用关键点：
   - 肩
   - 肘
   - 腕
   - 髋
   - 膝
   - 踝
   - 脚尖
4. 转换为统一 `PoseFrame`。
5. 实现低置信度过滤。
6. 实现骨架 Overlay 绘制。

完成标准：

1. 人体入镜时能看到骨架线。
2. 人体丢失时提示“请保持全身入镜”。
3. 识别结果不直接暴露 ML Kit 原始对象，而是统一转换为 `PoseFrame`。

## 第 8 步：实现基础数学工具和规则动作识别

目标：先不用模型，用规则识别深蹲、开合跳和未知动作。

需要实现 `pose/feature` 工具：

1. 三点夹角计算。
2. 两点距离计算。
3. 膝盖相对脚踝横向偏移。
4. 肩宽、髋宽、身高估算归一化。
5. 躯干倾角计算。
6. 关键点置信度检查。

需要实现 `RuleBasedActionClassifier`：

1. 输入连续若干帧 `PoseFrame`。
2. 根据髋、膝、踝角度和髋部高度变化判断深蹲趋势。
3. 根据手腕高度、脚踝距离和开合变化判断开合跳趋势。
4. 输出：
   - `SQUAT`
   - `JUMPING_JACK`
   - `UNKNOWN`
5. 置信度不足时输出 `UNKNOWN`。

完成标准：

1. 做深蹲时能稳定识别为 `SQUAT`。
2. 做开合跳时能稳定识别为 `JUMPING_JACK`。
3. 站立或离开画面时不误判为有效动作。

## 第 9 步：实现深蹲计数、评分和问题提示

目标：完成项目最核心的深蹲评估。

需要实现深蹲状态机：

1. `STANDING`
2. `DESCENDING`
3. `SQUATTING`
4. `RISING`
5. 回到 `STANDING` 后计数加一

需要判断的问题：

1. 下蹲深度不足。
2. 膝盖内扣。
3. 背部前倾明显。
4. 左右不对称。
5. 人体识别不稳定。
6. 节奏异常。

建议扣分规则：

1. 下蹲深度不足：扣 15 分。
2. 膝盖内扣：扣 20 分。
3. 背部前倾明显：扣 15 分。
4. 左右不对称：扣 10 分。
5. 人体识别不稳定：扣 10 分。
6. 动作节奏异常：扣 5 分。

需要输出建议：

1. 下蹲深度不足：适当增加下蹲幅度，让髋部接近膝盖高度。
2. 膝盖内扣：下蹲时保持膝盖朝向脚尖，避免向身体中线内扣。
3. 背部前倾：收紧核心，保持胸部打开，减少躯干过度前倾。
4. 左右不对称：注意左右脚均匀发力，保持身体中心稳定。

完成标准：

1. 正常深蹲能计数。
2. 故意下蹲太浅能提示下蹲深度不足。
3. 故意膝盖内扣能提示膝盖内扣。
4. 故意身体前倾能提示背部前倾。
5. 结束训练后能保存每次深蹲的结果。

## 第 10 步：实现开合跳基础计数

目标：完成扩展动作，增强课程展示效果。

需要实现开合跳状态机：

1. `CLOSED`
2. `OPENING`
3. `OPEN`
4. `CLOSING`
5. 回到 `CLOSED` 后计数加一

判断特征：

1. 双脚脚踝横向距离。
2. 左右手腕高度。
3. 手腕是否高于肩膀。
4. 脚踝距离是否明显大于肩宽或髋宽。

完成标准：

1. 正常开合跳能计数。
2. 显示训练时长。
3. 显示平均节奏。
4. 人体丢失时暂停计数并提示。
5. 训练结束后保存结果。

## 第 11 步：实现 Nearby 多设备组队和机位分配

目标：实现开题报告中的多手机协同亮点。

需要添加依赖：

1. Google Nearby Connections。
2. Gson 或 Moshi。
3. ZXing 或 ML Kit Barcode Scanning，用于二维码。

需要添加权限：

1. Android 12 以下通常需要位置权限配合 Nearby。
2. Android 12 及以上需要蓝牙扫描、广播、连接相关权限。

需要实现：

1. 主控端创建房间。
2. 主控端开启 advertising。
3. 节点端输入房间码或扫码。
4. 节点端开启 discovery。
5. 两台手机建立连接。
6. 主控端显示节点设备名、电量、延迟、在线状态。
7. 主控端分配 `FRONT_CAMERA` 或 `SIDE_CAMERA`。
8. 节点端收到角色后更新页面。

统一消息类型：

1. `JOIN_REQUEST`
2. `JOIN_ACCEPTED`
3. `DEVICE_STATUS`
4. `ASSIGN_ROLE`
5. `START_COUNTDOWN`
6. `START_ANALYSIS`
7. `POSE_FRAME`
8. `ANALYSIS_SUMMARY`
9. `END_TRAINING`
10. `HEARTBEAT`
11. `ERROR`

完成标准：

1. 两台安卓手机能真实连接。
2. 主控端能看到节点设备。
3. 主控端能分配正面/侧面机位。
4. 节点端能显示自己的机位角色。
5. 主控端能发送同步倒计时。

## 第 12 步：主控端和节点端联动

目标：把 Nearby、CameraX、ML Kit、规则分析串起来。

推荐 MVP 数据上传策略：

1. 节点端本地完成姿态识别和基础分析。
2. 节点端优先上传 `ANALYSIS_SUMMARY`，不要每一帧都上传完整图片。
3. 调试时可以抽样上传 `POSE_FRAME`。
4. 问题动作时保存关键帧路径或压缩图。

主控端需要显示：

1. 每个机位在线状态。
2. 人体是否在画面内。
3. 当前动作类型。
4. 当前次数。
5. 当前评分。
6. 主要问题提示。
7. 断连或低置信度提示。

完成标准：

1. 主控点击开始后，节点进入摄像头识别状态。
2. 节点分析结果能传回主控端。
3. 主控端实时更新次数和问题提示。
4. 某个节点掉线时，主控端能提示并降级处理。

## 第 13 步：完善结果页、历史记录和报告导出

目标：训练结束后能保存、查看、导出。

结果页需要显示：

1. 动作类型。
2. 总次数。
3. 训练时长。
4. 平均评分。
5. 合格次数。
6. 主要问题。
7. 改进建议。
8. 正面关键帧。
9. 侧面关键帧。

历史记录需要支持：

1. 按日期展示。
2. 按动作类型筛选。
3. 点击进入详情。
4. 删除训练记录。

报告导出需要实现：

1. JSON 摘要导出。
2. 可选关键点样本 JSON 导出，用于后续模型训练。
3. `PdfDocument` PDF 报告导出。
4. `ExportRecordEntity` 保存导出记录。

本地文件目录：

```text
files/training/{sessionId}/frames/
files/training/{sessionId}/videos/
files/training/{sessionId}/reports/
files/training/{sessionId}/raw_pose/
```

完成标准：

1. 训练结束后历史记录出现新数据。
2. 点击历史记录能看到详情。
3. PDF 报告能生成并保存路径。
4. JSON 可作为后续模型训练数据来源。

## 第 14 步：补齐测试和验收材料

目标：让项目不只是能跑，还能稳定答辩。

单元测试：

1. 三点夹角计算。
2. 距离归一化。
3. 深蹲状态机。
4. 开合跳状态机。
5. 评分扣分规则。
6. Room DAO 基本增删改查。

真机测试：

1. 登录注册。
2. 自动登录。
3. CameraX 打开摄像头。
4. ML Kit 识别人体关键点。
5. 骨架 Overlay 显示。
6. 深蹲计数和评分。
7. 开合跳计数。
8. 两台手机 Nearby 连接。
9. 角色分配和同步倒计时。
10. 结果保存和 PDF 导出。

验收材料：

1. 登录页截图。
2. 首页截图。
3. 创建设备房间截图。
4. 设备组队与机位分配截图。
5. 摄像头骨架截图。
6. 深蹲评估截图。
7. 结果页截图。
8. 历史记录截图。
9. PDF 报告样例。
10. 两台手机联动演示视频。

## 第 15 步：最后扩展 TFLite 动作分类模型

目标：主 APP 稳定后，再扩展十种以上动作识别，作为加分项。

注意：这一步不要阻塞前面的 APP 主流程。

建议动作类别：

1. 深蹲
2. 开合跳
3. 俯卧撑
4. 仰卧起坐
5. 弓步蹲
6. 高抬腿
7. 平板支撑
8. 波比跳
9. 站立
10. 未知动作

训练路线：

1. 从 APP 导出关键点 JSON/CSV。
2. 整理训练数据和标签。
3. 以连续 16 到 32 帧作为窗口。
4. 提取关键点坐标、关节角度、距离比例和时序变化。
5. 训练小型 MLP 或 1D CNN。
6. 导出：
   - `action_classifier.tflite`
   - `labels.txt`
   - `model_meta.json`
7. 放入 `app/src/main/assets/`。
8. 实现 `TFLiteActionClassifier`。
9. 模型失败、置信度过低或输入维度不匹配时自动 fallback 到 `RuleBasedActionClassifier`。

完成标准：

1. APP 不依赖模型也能完整演示。
2. 接入模型后能显示动作类型和置信度。
3. 能识别十种以上动作或至少保留十种标签扩展能力。
4. 模型只负责动作类型识别，评分仍由规则模块完成。

## 推荐开发顺序简表

| 顺序 | 阶段 | 目标 |
|---:|---|---|
| 1 | 工程基础 | XML/ViewBinding、包结构、构建配置 |
| 2 | 页面骨架 | 所有核心页面能跳转 |
| 3 | 公共模型 | 枚举、PoseFrame、分析结果、接口 |
| 4 | 数据层 | Room、DataStore、Repository |
| 5 | Fake 闭环 | 无摄像头也能保存训练结果 |
| 6 | CameraX | 真机摄像头预览和帧采集 |
| 7 | ML Kit | 姿态关键点和骨架 Overlay |
| 8 | 规则分类 | 识别深蹲、开合跳、未知动作 |
| 9 | 深蹲评估 | 计数、评分、问题提示 |
| 10 | 开合跳 | 基础计数、时长、节奏 |
| 11 | Nearby | 多设备连接、机位分配 |
| 12 | 联动 | 主控端和节点端实时同步 |
| 13 | 结果导出 | 历史记录、JSON、PDF |
| 14 | 测试验收 | 单元测试、真机测试、截图视频 |
| 15 | 模型扩展 | TFLite 十种以上动作识别 |

## 当前最应该先做的 3 件事

1. 把当前 Compose 单 Activity 壳迁回 XML/ViewBinding 页面骨架。
2. 先实现 Room + DataStore + 登录 + 历史记录，让数据能真实保存。
3. 用 Fake 数据跑通训练闭环，再接 CameraX 和 ML Kit。
